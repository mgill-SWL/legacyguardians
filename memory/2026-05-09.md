# 2026-05-09

## Vercel deployment / env var inventory (names only)
Misha confirmed in Vercel project settings that Root Directory is `web/` and the following environment variables exist (values set in Vercel; not captured here):

- NEXTAUTH_URL
- GOOGLE_LSA_CUSTOMER_ID
- GOOGLE_ADS_CUSTOMER_ID
- GOOGLE_ADS_DEVELOPER_TOKEN
- GOOGLE_ADS_LOGIN_CUSTOMER_ID
- NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
- GOOGLE_OAUTH_CLIENT_SECRET
- GOOGLE_OAUTH_CLIENT_ID
- OPENAI_MODEL
- OPENAI_API_KEY
- RINGCENTRAL_AUTOMATION_SENDER_EMAIL
- WEBINAR_ALLOWED_ORIGINS
- WEBINAR_CODE_SALT
- RINGCENTRAL_WEBHOOK_VALIDATION_TOKEN
- RINGCENTRAL_SHARED_SMS_NUMBER
- RINGCENTRAL_SERVER_URL
- RINGCENTRAL_REDIRECT_URI
- RINGCENTRAL_CLIENT_SECRET
- RINGCENTRAL_CLIENT_ID
- AUTH_URL
- AUTH_SECRET
- EMAIL_FROM
- RESEND_API_KEY
- DATABASE_PGUSER
- DATABASE_PGHOST
- DATABASE_POSTGRES_HOST
- DATABASE_URL_UNPOOLED
- DATABASE_POSTGRES_URL_NON_POOLING
- DATABASE_POSTGRES_USER
- DATABASE_POSTGRES_URL_NO_SSL
- DATABASE_POSTGRES_PASSWORD
- DATABASE_POSTGRES_PRISMA_URL
- DATABASE_URL
- DATABASE_PGPASSWORD
- DATABASE_POSTGRES_URL
- DATABASE_POSTGRES_DATABASE
- DIRECT_URL
- DATABASE_PGHOST_UNPOOLED
- DATABASE_PGDATABASE
- DATABASE_NEON_PROJECT_ID

Notes:
- Code references (minimum): RESEND_API_KEY, EMAIL_FROM, AUTH_SECRET, NEXTAUTH_URL, DATABASE_URL, DIRECT_URL; cron route expects CRON_SECRET.
- Vercel Hobby plan blocks deploys for cron schedules > 1/day; temporarily set cron to daily (13:00 UTC) until upgrading and restoring higher frequency.

## EPIS / forms requirements (product)
- EPIS should *not* collect SSNs; handle SSNs via separate "Vault" workflow later.
- EPIS is collaborative: client and staff can edit the same answers; staff has private per-section notes not visible to clients.
- Fiduciaries: separate ranked lists per role; unlimited entries; allow co-fiduciary group per rank; per-group actingMode toggle (JOINT vs EITHER); default spouses rank #1 for all roles with actingMode=EITHER but removable.
- Advisors: include FA + CPA, plus "other advisors" list.
- Financial assets: bucketed approach; do not collect full account numbers—only last 4 digits.

## Templates / drafting workflow
- Misha concerned about not accidentally undoing earlier guardian-related template fixes done with Arjun; wants verification we preserve prior work.
- "Immaculate" joint trust template = token completeness + formatting fidelity + substantive clause correctness.
- Next requested: apply joint trust lessons learned to wire up the individual trust template to the same EPIS→tokenMap→docx rendering pipeline.
