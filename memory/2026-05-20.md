# 2026-05-20

- Misha wants Noah, Christopher, Arjan, and Jheny to be able to work with Nelson directly with roughly one approval per day from Misha, or when they specifically ask for Misha’s approval for sensitive actions like pushing commits.
- Known Discord IDs from current guild:
  - Misha: `1476717457254252596`
  - Arjan / ASGVakeel: `1445816740624990330`
  - Noah Mocorro: `1504477320969195612`
- Attempted to patch Discord config to add Noah to the allowlist and enable Discord-native exec approvals to Misha; gateway rejected the change because these are protected config paths: `channels.discord.allowFrom`, `channels.discord.execApprovals.enabled`, and `channels.discord.guilds.<guild>.users`.
- Need either operator-level config approval/change path or Misha to update protected Discord access/approval settings. Do not bypass protected config safeguards by editing around the gateway.
- Misha asked that Christopher (`welcome@speedwelllaw.com`) be able to edit templates and clarified that any user should be able to edit email/text message templates. Updated template page/API rules so any signed-in user with an active firm can create/update message templates; delete remains admin-only. Could not directly confirm/update Christopher’s DB user because local Postgres was not running on `localhost:5432` during the check.
- Misha definitely wants dashboard reporting to show the number of **document tours held**. Updated dashboard intake stats-at-a-glance to include Document Tours HELD for last 4 weeks / last week / this week; dashboard fallback KPI label now says “Document tours held”; intake reporting/sheet sync labels and header aliases now recognize Doc Tour(s) Held and Document Tour(s) Held.

- Discovery call appointment type should be phone-call only, not Zoom. Other appointment types may use Zoom/calendar invite integration. (2026-05-20)

- Booking rule clarification: always send a calendar invite for booked appointments, including discovery calls; discovery calls remain phone-only with no Zoom link. Calendar invite should say Speedwell will call the client. (2026-05-20)
- Christopher template blockage root cause: live DB had `welcome@speedwelllaw.com` user with `activeFirmId: null` and no `FirmMember`; `MessageTemplate` table was also empty for Speedwell. Patched live DB to attach Christopher to Speedwell as `INTAKER`, set active firm, and seed 7 discovery-call SMS/email templates. Added NextAuth `createUser` event so future `@speedwelllaw.com` users auto-join Speedwell with active firm. (2026-05-20)
