# 2026-03-10

- Persistent API rate-limit issue (OpenAI) observed; mitigations applied via OpenClaw config.
  - Reduced concurrency: `agents.defaults.maxConcurrent=2`, `agents.defaults.subagents.maxConcurrent=1`.
  - Set subagents (`prd`, `security`, `ux`, `data`, `arch`, `coding`) primary model to `anthropic/claude-sonnet-4-5` with fallbacks `openai/gpt-5.2`, `anthropic/claude-opus-4-6`.
  - Despite this, rate-limit still hit instantly; user approved switching main/default primary model to Sonnet as well.
  - Updated `agents.defaults.model.primary` to `anthropic/claude-sonnet-4-5` with fallbacks `openai/gpt-5.2`, `anthropic/claude-opus-4-6` (main agent now uses Sonnet via defaults).

- Legacy-Guardians template hygiene work progressed:
  - Confirmed/encoded that selections can exist **per client** in joint representations.
  - Fixed drafting/token errors in Golden Templates: removed extra bracket in a child token; removed unintended leading space before `Email` in POA tokens.
  - Token inventory/mapping work in progress; current mapping file referenced: `data/token_mapping_v1.yml`.
  - Dictionary draft updated; adjusted trustee appointments structure to be keyed by trust: `appointments.trustees[i]` corresponds to `trusts[i]`.

- `openclaw doctor --non-interactive` noted a config migration hint: `browser.ssrfPolicy.allowPrivateNetwork` → `browser.ssrfPolicy.dangerouslyAllowPrivateNetwork`.
