# 2026-05-05 (Tue)

- Listed architecture docs: trust accounting data model/workflows/reports, permissions model, audit logging, security threat model, data classification, retention policy.
- Reviewed key trust accounting + permissions + audit docs; highlights: immutable posted ledger entries, maker/checker approvals, three-way reconciliation, SoD, step-up auth, append-only tamper-evident audit logs.
- Repo git status (porcelain) showed untracked files/dirs including `inbox/normalized/`, `inbox/tmp/`, `memory/.dreams/`, new memory files, `scripts/`, and new KPI/spec CSVs.
- Parsed April 2026 CosmoLex billing/collection-by-timekeeper uploads into raw CSVs and drafted KPI import/snapshot scripts plus Google Sheets specs.
- Captured firm employee roster + emails for KPI rollout; initial attorney set: Michael Gill, Alexandra Filiault, Arjan Grover, Omer Ozusta.
- Repo scan found actual app under `web/` (Next.js + Prisma + NextAuth). Prisma already includes a `GoogleConnection` model. Pivoted architecture toward upstream DB-backed financial events/bookkeeping feeding KPIs, with Google Sheets as reporting output only.
- Drafted `spec/kpi-upstream-system-plan.v1.md` and `spec/prisma-kpi-financial-model-draft.v1.md` to anchor next implementation slice.
- Correction from Misha: `Trust to General Transfer` / trust-to-operating transfer should count as a **collection event** for KPI/business purposes, even if bookkeeping still models it as a transfer between trust and operating.
- Received Trust Receipts Journal sample in CSV form. Clean columns: Date, Received From, Client, Purpose of Funds, Amount, Method. This is a strong primary source for `TRUST_DEPOSIT` events and validates the bookkeeping-first import approach.
- Received Trust Disbursements Journal CSV showing repeated `Trust to General Transfer` payments to Speedwell Law; this is now the clearest v1 source for trust-to-operating collection recognition. Trust Transfer Record sample for April returned `No data available`, but Misha clarified that report is specifically for matter-to-matter transfers and is a necessary capability even if infrequent.
- April General Ledger confirms mixed operating-side reality: trust-to-general transfers hit `4100:Fee Income`; some client money goes directly to `2320:Client General Retainer (Operating)`; some receipts are invoice-linked `--Split--` entries spanning fee income and reimbursed-cost lines (`4200`, `4250`). This means operating-side classification/reconciliation will need explicit rules, not just raw bank import.
- Misha confirmed KPI rule: only `4100:Fee Income` counts toward attorney revenue / collected-revenue KPIs. `4200` and `4250` should be tracked separately and excluded.
- Additional operating-side clarifications: card fees were historically eaten by the firm but CosmoLex Pay can now pass them to clients; refunds should reverse collected KPI credit in the future period when they occur; one-off services are often tracked under a single `General Matters` matter with invoice/payor-level detail; future check-printing support is needed for 8.5x11 custom checks with two receipt stubs.
- Important operating nuance: credit-card processor deposits can bundle multiple matters together. Those deposits are manually transcribed into CosmoLex and reconciled later. Processor fees are not netted out of each deposit; instead, the fee withdrawal hits separately once monthly at the beginning of the month.
- Invoice Payment Allocations (all 4 sheets) turned out to be the key operating-side source for KPI recognition. It bridges applied date, applied amount, matter owner, invoice, client-matter, source of funds (`From Trust`, `New Payment`, `Operating Retainer`), and account-level decomposition (`4100`, `4200`, `4250`). Working rule: use Applied Date as collected-KPI recognition date and count only the `4100` portion toward attorney revenue.
- Operating Retainer By Matter should be treated as a liability-ledger source. Negative balances likely indicate over-application / deficit at the operating-retainer liability level, not necessarily a negative matter-economic result.
- Strategic pivot: Misha wants Legacy Guardians, not CosmoLex, to become the bookkeeping source of truth. Bank/card feed or CSV imports plus in-app categorization/review should drive normalized financial events; Cosmolex becomes reconciliation/supporting input rather than primary source.
- Drafted `spec/prisma-transaction-inbox-model-draft.v1.md` for the bank-first transaction inbox backbone: raw transactions, import batches, review queue, and classification layer feeding normalized financial events.
