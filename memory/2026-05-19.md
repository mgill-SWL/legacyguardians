# 2026-05-19

- Implemented Legacy Guardians task management foundation: firm-scoped Task model with optional matter link, creator, assignee, deadline, narrative description, and 0–100 completionPercent constrained to increments of 10.
- Added `/tasks` staff tab, `/api/tasks` create/list, `/api/tasks/[taskId]` update/delete, dashboard open-task widget, sidebar Tasks nav item, and linked-task display on matter pages.
- Applied migration `20260519123000_tasks_foundation` to Neon DB; schema up to date.
- Task billing decisions from Misha: task billing categories should be **billable**, **billed**, **non-billable**, and **no charge**. Billable tasks require a linked matter before completion. Completing a billable task should eventually prompt Billing Assistant/GPT to suggest timecard narrative, duration/time, billable classification, billing code/category, and flag vague/risky narratives; assignee reviews/edits/approves, creating a **draft timecard** for later billing review/invoice assembly.
- Naming decision: left-nav tab for native GPT-like agents should be **Assistants**.
- Matter operating-system needs: custom matter fields as admin setting, fields populated from forms, matter timeline, manual action logging with types Phone call/Text/Email/Meeting/Internal note/Other, automations scheduled/run should appear in timeline.
- Custom matter field types wanted: text, long text, date, currency, number, True/False, picklist, multi-select picklist, user/staff member, contact/person, and likely Lawmatics-style lookup (relationship to another object/record).
- Implemented Matter Timeline foundation: `MatterTimelineEvent` model + migration `20260519143000_matter_timeline`, manual timeline logger on matter pages, `/api/matters/[matterId]/timeline`, and automatic events for task created, task completed, draft timecard created, and pipeline stage changed. Migration applied; build passed.
- Implemented custom matter fields foundation: `MatterFieldDefinition` and `MatterFieldValue` models + migration `20260519151500_custom_matter_fields`, admin creation UI in Firm Settings, API routes for definitions and matter values, rendering/editing on matter pages, and `MATTER_FIELD_UPDATED` timeline event. Migration applied; build passed.
- Contact directory decision: contacts should cover professional advisors/general contacts, not just clients/vendors/referrers. Financial advisors are a major referral source. Implemented ContactCategory enum additions `PROFESSIONAL_ADVISOR` and `GENERAL` with migration `20260519205000_contact_categories_advisors`, updated contacts page/API/filter/create form. Build passed.
- Expanded professional/referral contact foundation: added `ContactProfessionalType` (FINANCIAL_ADVISOR, CPA, INSURANCE, BANKER, REALTOR, CARE_MANAGER, ATTORNEY, OTHER), `ReferralSourceStatus` (PROSPECT, ACTIVE, INACTIVE), contact `relationshipOwner`, and matter `referralSourceContact`; updated contacts create/list UI and matter pipeline fields form with referral source selector. Migration `20260519210500_professional_contacts_referrals` applied; build passed.
