# 2026-05-15

- Notary sections across all documents need to be “wired up” so they can change based on attorney-user input (likely template tokens/conditionals vs hardcoded blocks).
- There’s an issue where an extra blank underline remains in notary blocks; suspected cause is template structure (e.g., the underline is literal Word content, while the adjacent name/token resolves to empty) rather than the token replacement itself.
- renderTemplate.ts sanitizes Word XML before docxtemplater render: fixes some unclosed tokens like "[[TOKEN]," (common in notary blocks), strips remaining Jinja tags/vars, and uses docxtemplater delimiters [[...]] with nullGetter returning "" for missing tokens.
