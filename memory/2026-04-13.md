
## Deployment/debug notes (Vercel + RingCentral)
- Vercel deploy initially failed due to TypeScript checking against stale Prisma Client; errors referenced missing prisma.crmTask and missing include.ringCentralConnection types.
- Fix: updated web/package.json build script to run `prisma generate && next build` (commit b437df5). This allowed deploy to succeed.
- After deploy, RingCentral connect failed with 500 at `/api/ringcentral/oauth/start` in production; most likely missing RingCentral env vars in Vercel (RINGCENTRAL_CLIENT_ID/SECRET/REDIRECT_URI/SERVER_URL). Need to set env vars in Vercel Production and redeploy.
