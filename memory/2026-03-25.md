# 2026-03-25

## Summary / accomplishments
- Diagnosed magic-link email not arriving: NextAuth EmailProvider is configured to send via Resend, but the sender domain was not verified in Resend.
- Confirmed `web/src/authOptions.ts` overrides `sendVerificationRequest()` to use Resend and `.env.local` includes `RESEND_API_KEY` and `EMAIL_FROM`.
- Resolution path:
  - Temporary unblock: use Resend onboarding sender (e.g., `onboarding@resend.dev`) for `EMAIL_FROM`.
  - Proper fix: verify `legacyguardians.ai` in Resend (DNS SPF/DKIM/verification), then set `EMAIL_FROM=no-reply@legacyguardians.ai`.
- Follow-up: after verifying the domain in Resend/GoDaddy and restarting the Next.js dev server, the magic link email flow worked.

## Notes
- User is working on Resend auto-configure, which is slow.

## Process / assistant ops (user request)
- User asked: each day, write a summary log of important accomplishments/conversations and, at the start of each day’s chat, refresh context from those logs.
