# 2026-05-18

- Bookkeeping priority decision: start with **Operating vendor checks** (check register + match-to-bank outflows) before P&L.
- Desired workflow (CosmoLex-like): on Withdrawal→Method=Check, show check number dialog with **"To be printed"** (default checked). If to-be-printed is unchecked, user can **manually enter** check # (digits). If checked, show placeholder ("To print") and **assign the next check # per bank account at print time** (not at save time).
- Plan: add first-class OperatingCheck model + **per-FinancialAccount** check-number sequence; support statuses DRAFT/ISSUED/CLEARED/VOID and clearing via linking to imported bank CSV transactions (rawData.checkNumber).
- User accounts noted: Burke & Herbert operating; South State money market checking (own check stock + its own check # sequence); South State line of credit + credit card (liability-side running balances needed).
- CosmoLex check example PDF: landscape check + voucher/stub; prints date/payee/amount numeric+words/check# plus memo lines and COA-like coding (e.g., 6012:Contract Labor) and account name; MICR line not visible in extracted text → likely pre-printed MICR stock.
