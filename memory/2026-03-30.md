## 2026-03-30

- Set up LegacyGuardians MVP web app on Vercel w/ GoDaddy DNS: `app.legacyguardians.ai` CNAME -> `cname.vercel-dns.com`. Root domain parked.
- Next.js app in `/web` with Speedwell-style design tokens (`web/src/design/tokens.css`) and dashboard landing.
- Auth: NextAuth email magic-link using Resend; sign-in restricted to `@speedwelllaw.com` in `web/src/authOptions.ts`.
- Implemented Matter creation MVP:
  - `/matters/new` bare-minimum intake (grantors, children list, successor trustees, distribution scheme).
  - Explicit checkbox `hasMinorChildren` to conditionally include minors docs.
  - Stores intake JSON in Prisma `IntakeResponse`.
- Binder plan established and documented at `spec/binder-plan.md` with canonical signing order:
  1) Joint Trust (incl Exhibit A Standard Trustee Powers)
  2) Last Will & Testament (two files, one per spouse)
  3) Advance Medical Directive (two files)
  4) Burial POA = “Final Disposition Authorization and Instructions” (two files)
  5) General Durable POA (incl agent ack/certs embedded)
  6) Certification of Trust (+ Exhibit A appended)
  7) Assignment of Tangible Personal Property
  8) Declaration of Trust
  9) Instructions for TPP distribution (often left blank at signing)
  10) Summary of Client Information (SCI) (first client deliverable)
  11) Summary of Estate Planning Provisions
- Packet generation endpoint: `POST /api/matters/[matterId]/generate` produces ZIP packet. Placeholders were initially invalid .docx; fixed by generating valid placeholder DOCX via `docx` library (`web/src/lib/docx/placeholderDocx.ts`).
- Split templates out of `templates/source/Golden Template - Joint Trust.docx` into `templates/canonical/packet_split/`:
  - wills (client1/client2)
  - AMD (client1/client2)
  - Final Disposition (client1/client2)
  - minors POA/healthcare doc (conditional)
  - certification/assignment/declaration extracted (some wiring pending)
- Debugged docxtemplater “Multi error”: cause was broken hybrid tokens like `[[firstalternateguardianrelationshiptospouse1]/{{ ... }}`.
  - Patched minors split template, then discovered same broken tags existed in `templates/canonical/joint.docx`; patched that too.
  - Added build SHA to error payloads and manifest to debug Vercel deployment.
- Replaced/stripped Jinja-style `{{ ... }}` tokens in `templates/canonical/joint.docx` for MVP:
  - Replaced core trust name + client name tokens with `[[ClientTrustName]]`, `[[Client1FullName]]`, etc.
  - Later stripped remaining Jinja tokens entirely (left blanks for complex loops/appointments).
- Trust name default rule (MVP): `THE [Client1 surname] FAMILY LIVING TRUST` as title; fixed double “THE” by ensuring generated `ClientTrustName` omits leading “THE” since template includes it.
