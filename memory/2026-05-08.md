## 2026-05-08
- Approvals friction: user wants to reduce exec approvals; plan is to relax approvals selectively (option B).
- OpenClaw config changes discussed/applied:
  - tools.exec.ask set to "on-miss" (approve once per new command; repeats don’t reprompt).
  - tools.exec.strictInlineEval set to true (still forces approvals for inline eval like node -e / python -c / osascript -e).
  - tools.exec.safeBins includes "rg" to allow ripgrep without approvals.
- Noted that Discord has its own exec approval gating via channels.discord.execApprovals; user requested relaxing Discord exec approvals for their DM.
- Product direction: user wants **timekeeping + invoicing + A/R in LG** (not CosmoLex).
- Flat-fee billing preference: flat fees are captured at the **timecard/time-entry level** (pricingMode=FLAT, amount set on the entry, duration 0.0), e.g. “Will Base Price” = $2,800.
- Invoice numbering: include a **4-char firm slug prefix** (e.g., SWL), and number invoices **per firm per year**.
- Reserved MSO firm slug: **LG** (available; not yet created as a Firm).
- Merrifield: treat as a **FirmLocation** (not separate Firm); want P&L by location. Expect users to belong to exactly one location. Assigned: Misha+Noah -> Alexandria; Arjan -> Merrifield. Added MSO as a location for attribution. Backfilled existing matters: set firmId for 1 matter; set primaryLocationId for 13 matters to Alexandria.
- Ownership: Firm owners are **person-only**; ownership changes are **effective-dated** with percentages.
- MSO model: need an effective-dated firm-level revenue split to Legacy Guardians (0% for SWL, ~15–30% for some future firms). Split base should be net after refunds/chargebacks and **exclude advanced client costs**; invoices usually mix fees + advanced cost reimbursements. Default payment allocation should be **advanced-client-costs-first**. Implemented invoice line typing + allocation logic accordingly.
- Bookkeeping/accounting area status snapshot:
  - /web/src/app/(authed)/management/accounting/page.tsx is a placeholder.
  - Prisma schema includes financial models/enums: BillingAccount, FinancialAccount, RawFinancialTransaction, TransactionReviewItem, FinancialClassification, KpiImportBatch, MatterFinancialEvent.
  - Admin import pages/routes exist for trust receipts, trust disbursements, and operating retainer; they create KpiImportBatch + MatterFinancialEvent rows.
  - Spec acceptance criteria for trust accounting includes AC16–AC20 (trust ledger, reports, reconciliation workflow, audit trail, CosmoLex push).
